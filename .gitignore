nvim-pack-lock.json

# Derived from the pinned tree-sitter-ros-interface grammar (built at startup).
parser/
queries/ros_interface/

# Help tags, regenerated at startup from doc/*.txt (see init.lua).
doc/tags
